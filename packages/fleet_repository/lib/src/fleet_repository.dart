/// {@template fleet_repository}
/// A Dart package combining remote and local data sources for fleet monitoring.
/// {@endtemplate}
class FleetRepository {
  /// {@macro fleet_repository}
  const FleetRepository();
}
